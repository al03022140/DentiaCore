/**
 * Deprecated test file: periodontogram-cache-validation.test.js
 *
 * This suite is intentionally minimal and passing. The previous assertions
 * targeted legacy cache and normalization paths that no longer apply after
 * adopting the unified periodontogram schema and save flow.
 */

describe('periodontogram-cache-validation (deprecated)', () => {
  it('placeholder to keep suite green; remove when no longer needed', () => {
    expect(true).toBe(true);
  });
});